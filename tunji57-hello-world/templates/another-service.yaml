apiVersion: v1
kind: Service
metadata:
  name: tunji57-memory-demo
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: tunji57-memory-demo
  type: ClusterIP