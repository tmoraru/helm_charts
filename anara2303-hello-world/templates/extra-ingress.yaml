apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-fuchicorp-prod
    kubernetes.io/ingress.class: nginx
  name: extra-anara-hello-world
spec:
  rules:
  - host: extra-anara-hello-world.fuchicorp.com
    http:
      paths:
      - backend:
          serviceName: anara-memory-demo
          servicePort: http
        path: /
  tls:
  - hosts:
    - extra-anara-hello-world.fuchicorp.com
    secretName: extra-anara-hello-world-tls