apiVersion: v1
kind: Service
metadata:
  name: enver0173-memory-demo
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: enver0173-memory-demo
  type: ClusterIP