apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-fuchicorp-prod
    kubernetes.io/ingress.class: nginx
  name: extra-ktalant-hello-world
spec:
  rules:
  - host: extra-ktalant-hello-world.fuchicorp.com
    http:
      paths:
      - backend:
          serviceName: ktalant-memory-demo
          servicePort: http
        path: /
  tls:
  - hosts:
    - extra-ktalant-hello-world.fuchicorp.com
    secretName: extra-ktalant-hello-world-tls