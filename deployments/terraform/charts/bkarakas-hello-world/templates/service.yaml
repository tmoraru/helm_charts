apiVersion: v1
kind: Service
metadata:
  name: {{ include "bkarakas-hello-world.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "bkarakas-hello-world.name" . }}
    helm.sh/chart: {{ include "bkarakas-hello-world.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bkarakas-hello-world.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
