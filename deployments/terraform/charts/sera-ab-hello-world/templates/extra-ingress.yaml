apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-fuchicorp-prod
    kubernetes.io/ingress.class: nginx
  name: extra-sera-ab-hello-world
spec:
  rules:
  - host: extra-sera-ab-hello-world.fuchicorp.com
    http:
      paths:
      - backend:
          serviceName: hakten-memory-demo
          servicePort: http
        path: /
  tls:
  - hosts:
    - extra-sera-ab-hello-world.fuchicorp.com
    secretName: extra-sera-ab-hello-world-tls