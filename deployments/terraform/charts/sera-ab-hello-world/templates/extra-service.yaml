apiVersion: v1
kind: Service
metadata:
  name: hakten-memory-demo
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: hakten-memory-demo
  type: ClusterIP