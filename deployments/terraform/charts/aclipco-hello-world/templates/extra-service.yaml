apiVersion: v1
kind: Service
metadata:
  name: aclipco-memory-demo
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: aclipco-memory-demo
  type: ClusterIP