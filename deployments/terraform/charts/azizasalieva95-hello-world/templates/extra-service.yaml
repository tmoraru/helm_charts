apiVersion: v1
kind: Service
metadata:
  name: azizasalieva95-memory-demo
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: azizasalieva95-memory-demo
  type: ClusterIP