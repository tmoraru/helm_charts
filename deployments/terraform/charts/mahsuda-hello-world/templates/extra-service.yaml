apiVersion: v1
kind: Service
metadata:
  name: mahsuda-memory-demo
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: mahsuda-memory-demo
  type: ClusterIP