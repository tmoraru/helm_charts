apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-fuchicorp-prod
    kubernetes.io/ingress.class: nginx
  name: extra-mahsuda-hello-world
spec:
  rules:
  - host: extra-mahsuda-hello-world.fuchicorp.com
    http:
      paths:
      - backend:
          serviceName: mahsuda-memory-demo
          servicePort: http
        path: /
  tls:
  - hosts:
    - extra-mahsuda-hello-world.fuchicorp.com
    secretName: extra-mahsuda-hello-world-tls