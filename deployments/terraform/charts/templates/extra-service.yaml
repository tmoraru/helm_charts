apiVersion: v1
kind: Service
metadata:
  labels:
  app: Ulan-devops-memory-demo
  name: Ulan-devops-memory-demo
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: Ulan-devops-memory-demo
  type: ClusterIP
