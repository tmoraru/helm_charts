apiVersion: v1
kind: Service
metadata:
  name: aidinkobonov-memory-demo
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: aidinkobonov-memory-demo
  type: ClusterIP