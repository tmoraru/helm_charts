apiVersion: v1
kind: Service
metadata:
  name: rootvovak-memory-demo
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: rootvovak-memory-demo
  type: ClusterIP