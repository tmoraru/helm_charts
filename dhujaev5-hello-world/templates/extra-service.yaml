apiVersion: v1
kind: Service
metadata:
  name: dhujaev5-memory-demo
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: dhujaev5-memory-demo
  type: ClusterIP